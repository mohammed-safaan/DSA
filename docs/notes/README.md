# Learning Notes

Notes from my course work, written as I learn — not copied from anywhere. Each note explains the topic in my own words, then ends with a quick-reference table so I can re-scan it fast.

## Contents

| Note | Topics | Links to |
| ---- | ------ | -------- |
| [01 — Complexity Fundamentals](01-complexity-fundamentals.md) | exponentiation, factorial, logarithms, Big O, `O(log n)`, `O(n)`, `O(n log n)`, `O(nm)`, `O(n²)` | `solutions/binary-search/`, `solutions/sorting/` |

## How notes and solutions connect

Notes explain **why** — the concepts behind the complexity classes. Solution folders in `solutions/` show **how** — working implementations of each algorithm, with their own approach write-ups.
